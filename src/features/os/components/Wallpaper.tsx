"use client";

import React from "react";

export function Wallpaper() {
  // We rely on the global MatrixBackground for visuals.
  // Returning null keeps the OS background transparent so the matrix rain shows through.
  return null;
}
